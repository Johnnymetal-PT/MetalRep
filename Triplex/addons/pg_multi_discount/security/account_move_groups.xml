<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="group_account_multi_discount_security" model="res.groups">
        <field name="name">Multi-Discount on Invoice</field>
        <field name="users" eval="[(4, ref('base.user_admin'))]" />
        <field name="comment">This user will be able to display multi-discount on Invoice</field>
    </record>

</odoo>
